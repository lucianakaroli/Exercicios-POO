/* Agora, para a funcao max(array) implementada no exercicio anterior, procure na API de JavaScript alguma funcao
ou metodo de algum objeto que poderia auxiliar no calculo desejado. Mostre um exemplo de codigo utilizando o que voce
encontrou para realizar o mesmo calculo do exercicio anterior. */

const numeros = [1,2,2,3,1,9,4,3];
console.log(Math.max(...numeros));